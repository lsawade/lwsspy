


def readParfile(filename: str) -> dict:
    """Reads in a ``Par_file`` and returns a dictionary containing the
    parameters.

    Args:
        filename (str): Path to Par_file

    Returns:
        dict: dictionary with all parameters
    """